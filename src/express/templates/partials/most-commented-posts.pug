section.main-page__hot.hot
  h2.hot__name
    | Самое обсуждаемое
    span.hot__icon.hot__icon--fire
  ul.hot__list
    each article in mostCommentedAtricles
      li.hot__list-item
          a.hot__list-link(href=`/articles/${article.id}`) #{formatArticleTitle(article.title, maxTitleLength)}
            sup.hot__link-sup #{article.commentsCount}
